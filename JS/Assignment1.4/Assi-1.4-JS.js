var msg;
msg = "The script is located in external script file called math.js ";
function addNumbers(headParam, bodyParam) {
  var sum = headParam + bodyParam;
  document.write(
    "Addition of " + headParam + " and " + bodyParam + " is :" + sum
  );
  /* display the contents of the variable "msg" */
  /* display the addition of two numbers */
}
